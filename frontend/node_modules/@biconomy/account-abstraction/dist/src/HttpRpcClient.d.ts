import { UserOperation, UserOpGasFields } from '@biconomy/core-types';
export declare class HttpRpcClient {
    readonly bundlerUrl: string;
    readonly entryPointAddress: string;
    readonly chainId: number;
    readonly dappAPIKey: string;
    private readonly userOpJsonRpcProvider;
    constructor(bundlerUrl: string, entryPointAddress: string, chainId: number, dappAPIKey: string);
    validateChainId(): Promise<void>;
    sendUserOpToBundler(userOp1: UserOperation): Promise<any>;
    getUserOpGasAndGasPrices(userOp: Partial<UserOperation>): Promise<UserOpGasFields>;
    estimateUserOpGas(userOp1: Partial<UserOperation>): Promise<string>;
    private printUserOperation;
}
